<%- include('partials/head') %>
<%- include('partials/header') %>

<!-- Animated Blobs -->
<%- include('partials/blobs') %>

<!-- Page Hero -->
<div class="page-hero">
  <div class="container">
    <div class="breadcrumb">
      <a href="/research">Research</a> <span>/</span> Foundational Principles
    </div>
    <div class="page-hero-title">
      <h1><%= pageData.title %></h1>
    </div>
    <div class="page-hero-inner">
      <div class="page-hero-text">
        <p><%= pageData.description %></p>
      </div>
      <div class="page-hero-image">
        <img src="<%= pageData.heroImage %>" alt="<%= pageData.title %>">
      </div>
    </div>
  </div>
</div>

<!-- Research to Design Pipeline -->
<div class="pipeline-section">
  <div class="container">
    <div class="pipeline-header">
      <h2><%= pageData.pipelineTitle %></h2>
      <p><%= pageData.pipelineSubtitle %></p>
    </div>

    <div class="pipeline-steps">
      <% pageData.steps.forEach(function(step, i) { %>
      <div class="pipeline-step">
        <div class="step-number">0<%= i + 1 %></div>
        <div class="step-title"><%- step.title %></div>
        <% if (step.subtitle) { %>
        <div class="step-subtitle"><%= step.subtitle %></div>
        <% } %>
        <p class="step-desc"><%= step.description %></p>
        <% if (step.quote) { %>
        <div class="step-quote"><%- step.quote %></div>
        <% } %>
        <% if (step.bullets && step.bullets.length) { %>
        <ul class="step-list">
          <% step.bullets.forEach(function(b) { %>
          <li><%- b %></li>
          <% }) %>
        </ul>
        <% } %>
        <% if (step.deliverables && step.deliverables.length) { %>
        <div class="step-deliverable">
          <div class="step-deliverable-title"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 4h2a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h2"/><rect x="8" y="2" width="8" height="4" rx="1" ry="1"/></svg> Deliverable</div>
          <ul class="step-list">
            <% step.deliverables.forEach(function(d) { %>
            <li><%- d %></li>
            <% }) %>
          </ul>
        </div>
        <% } %>
      </div>
      <% }) %>
    </div>
  </div>
</div>

<!-- Case Studies -->
<div class="case-studies">
  <div class="container">
    <div class="case-studies-header">
      <h2>Case Studies</h2>
      <p>Real-world applications of our foundational principles in enterprise and research contexts.</p>
    </div>

    <% pageData.caseStudies.forEach(function(cs) { %>
    <div class="case-study-card">
      <h3><%= cs.title %></h3>
      <p><%= cs.description %></p>
      <div class="case-study-themes">
        <% cs.themes.forEach(function(t) { %>
        <span><%= t %></span>
        <% }) %>
      </div>
    </div>
    <% }) %>

  </div>
</div>

<!-- Build with SDK -->
<div class="sdk-section">
  <div class="container">
    <div class="sdk-inner">
      <div class="sdk-text">
        <h2>Build with the Hax SDK</h2>
        <p>The HAX SDK gives developers everything they need to integrate agents into their apps, without losing clarity, structure, or control. Use structured schemas, prebuilt components, and clear boundaries to keep agent behavior collaborative and predictable.</p>
        <a href="#"><button>Explore the SDK</button></a>
      </div>
      <div class="sdk-image">
        <img src="/images/research/sdk-hero.png" alt="Hax SDK">
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
